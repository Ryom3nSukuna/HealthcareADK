# Environment / secrets
.env

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
dist/
build/

# IDE
.vscode/
.idea/

# Data files (generated — too large for source control)
landing_zone/
client/landing_zone/

# Power BI binary (locked file may remain at old path during move)
powerbi/*.pbix
client/powerbi/*.pbix

# Schema knowledge base (rebuilt from live DB)
client/docs/schema_kb.json

# Claude Code local settings (machine-specific permission overrides)
.claude/settings.local.json

# SSIS Visual Studio local state and build artifacts
client/ssis/**/.vs/
client/ssis/**/obj/
client/ssis/**/bin/
